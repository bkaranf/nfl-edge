# R01 independent isolation review, cycle 1

Fresh native Sol `gpt-5.6-sol` / `xhigh`, session
`01a0a7ed-0f21-7b23-a089-a7bcde2f3f7e`. Reviewed I01 only.
Decision: **REQUEST REPAIR: durable regression coverage**. No implementation
failure found by independent probes.

1. The committed isolation test's sentinel was not an existing database at the
   exact default path. Add exact-path bytes/mtime protection on import and
   collect-only subprocess discovery; retain absent-directory coverage.
2. The collector cleanup in try/finally had no source test with a live synthetic
   collector and exception through lifespan. Add that regression and preserve
   the original exception.

Independent commands all ultimately exit 0:

- Focused source tests: 4 passed, `.qa/R01/targeted.xml`.
- Full source suite: 25 passed, `.qa/R01/full.xml`.
- Reviewer probes: 4 passed, `.qa/R01/probes-final.xml` and
  `.qa/R01/review_probes.py`. They cover exact existing default sentinel,
  collect-only discovery, documented app lifespan/health and exceptional cleanup.

The first reviewer-probe run timed out because its synthetic collector remained
disabled by NFL_EDGE_NO_COLLECT. The corrected probe clears that variable only
for its network-free fake; initial evidence remains `.qa/R01/probes.xml`.
No migration, WAL recovery, real database, network or release-ready claim.
## Cycle 2: ACCEPT

Author repaired only tests/test_isolation.py. Reviewer independently verified:
7 focused source tests, 4 independent probes, collect-only 28 cases with external
guard database absent, and clean diff checks. Evidence:
`.qa/R01/cycle2-focused.xml`, `cycle2-probes.xml`, `cycle2-collect.xml`.
Reviewed test SHA-256:
`9C881BF4EB228CA4F0270748502550DD5D187BD71F6F1C092A6F41E8D372188D`.
No new I01 findings. Full integration waits for financial writer completion.

Actual Windows Uvicorn smoke also passed with a uniquely named `.qa/R01` database,
collection disabled and loopback port 56082. Health returned expected JSON and
shutdown completed. Windows CTRL_BREAK returned child code 3 after graceful
shutdown; wrapper exit 0 validated shutdown logs, not merely exit status.
Evidence: `.qa/R01/uvicorn-smoke-result.json`, `uvicorn-smoke.stderr.log`,
`uvicorn-smoke.stdout.log`. Earlier wrapper attempts are retained as harness
failures (hidden-window CTRL_BREAK and overly strict exit-code assumption).
No child process remains. This is not the complete A26 delivery check.
