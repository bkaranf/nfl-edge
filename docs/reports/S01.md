# S01 financial contract investigation

Native Sol `gpt-5.6-sol` / `xhigh`, session
`01a0a7e7-f8c4-7a43-9d3b-12a75cf5f635`; base `20e954b`.
Read-only investigation completed. Agreed interface is in `docs/CONTRACTS.md`.

Confirmed defects with synthetic pure-engine reproductions:

- F1: q=.74, W=100, C=70.60, explicit T=0 currently qualifies with EV=2.20;
  the engine substitutes T=50. Correct 5% tie scenario EV is -0.30 with L=0.
- F2: integer adjacent-line fixture win=.4, push=.1 and EV=.8 currently
  qualifies. It must remain useful research with qualification disabled.
- Returns omit L, use float cash and display binary C/W for multi-outcome cases.
- No versioned profile admission; generic booleans are sufficient for a pass.
- Integer aggregation/dispersion lacks coherent full-vector treatment.
- Closing shares the same payoff function and must receive the financial fix.

Desired regression set: A01-A09 in the authoritative final plan, explicit
canonical/legacy contradictions, missing L/T, no production admitted profile,
positive admitted synthetic half-point fixture, integer containment and joint
vectors, period/profile mismatches, sign symmetry and cent boundary tests.

The scout ran a safely isolated suite after I01: 25 passed, 2 existing warnings,
exit 0. This verifies baseline execution only, not repaired financial behavior.
No repository edits or source network requests were made in S01. The same Sol
session was subsequently assigned I02 implementation, so it cannot independently
review I02; that requires a fresh reviewer context.
