# I01 test/default-database isolation

Author: native Sol `gpt-5.6-sol`, `xhigh`, session
`01a0a7e8-371f-7742-aaf2-d5f855f66edc`. Base: `20e954b`.
Status: implemented and accepted by fresh R01 reviewer after one test repair.

Write set: backend/app.py initialization/lifespan only, tests/conftest.py,
tests/test_isolation.py. Explicit database factories remain eager for fixture
seeding; the default import defers Store/Service initialization until lifespan.
Collector cancellation uses try/finally. Test collection installs a per-process
temporary NFL_EDGE_DB with collection disabled before importing test modules.

Author verification, exit 0 for each:

- `.venv\Scripts\python.exe -m pytest -q tests\test_isolation.py`: 4 passed.
- `.venv\Scripts\python.exe -m pytest --collect-only -q -p no:cacheprovider`:
  25 collected.
- `.venv\Scripts\python.exe -m pytest -q -p no:cacheprovider`: 25 passed.
- `git diff --check -- backend/app.py`: passed; CRLF conversion advisory only.

The author reports no production/default DB access, network, installs or
out-of-scope changes. Independent review and integrated evidence are separate
gates. This patch does not verify migrations or WAL-safe recovery.

Repair cycle added exact fake default-path sentinel import and collect-only
protection, plus synthetic collector exception/cancellation coverage. Seven
focused source tests pass; independent reviewer probes also pass. See R01.md.
