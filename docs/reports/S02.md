# S02 source, temporal and identity scout

Native Sol `gpt-5.6-sol` / `xhigh`, session
`01a0a7eb-f6c4-7a53-ac8f-d2d24f61c56a`; base `20e954b`.
No source/repository/database/network changes. Accepted design: CONTRACTS C06-C08.

Pure synthetic reproductions:

- ESPN moneyline OFF followed by valid spread/total returned one game, zero
  quotes and one skipped event; event-wide exception handling drops good markets.
- Malformed Bovada moneyline decimal similarly removed later valid total quotes.
- Same DET@BUF rescheduled by one day changed the date-based ID, splitting
  journal/exposure/cutoff identity.
- Opposite-side quotes with conflicting period/rules but matching batch formed
  one eligible pair. Pairing checks omit those semantics and admission.

Code-traced defects: batch-time historical queries exclude genuine pre-kickoff
receipts stored later; manual backdating can create false historical availability;
first closing miss becomes permanent; partial source refresh loses failed scopes;
no Retry-After/source circuit; five-minute background collection conflicts with
on-demand policy; provider IDs and schedule revisions are not durable.

Mapped proof requirements: A14 strict receipt-vs-record cutoff; A15 negative ROI
can be SUPPORTED, disagreement cannot, grace/corrections; A18 legacy unknown
provenance and immutable evaluation; A19 parser/board isolation; A20 failure,
full-empty/suspension, partial scopes, 403/429; A21 stable rescheduled links and
ambiguity quarantine. A16/A17 rely on the separate storage transaction contract.

No live permission/freshness/contract validation or browser/Windows release
verification was claimed. Initial policies must remain non-eligible and network
collection disabled pending documented access qualification.
