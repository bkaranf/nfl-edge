# R02 independent financial review

Fresh native `gpt-5.6-sol` / `xhigh`, default role, openai provider; session
`01a0a808-27ab-7890-946f-83d6c3fc4874`. Root and reviewer verified actual metadata.
Base `20e954bb175eaa8f3966ccf0327bb0b4476008bd` plus frozen I02; financial hashes
matched the author packet. Reviewer wrote only ignored `.qa/R02` probes/evidence.

**Final verdict: ACCEPT I02 financial scope.** The following cycle notes are
historical; their pending/open statements are superseded by this final verdict.
Final native turn `01a0a82b-4152-7f10-9f95-24c5148012c4` remained Sol/xhigh.
All three independent closing probes passed, along with 14 targeted closing
tests, compile checks, and diff checks. Final frozen production/test hashes
matched. The complete preflight table resolved the missing-return cause residual.
Root's final phase-1 and push-checkpoint suites both passed 81 tests.
This acceptance excludes source admission/quality, receipt cutoffs, finalization,
lifecycle persistence, real screening, and profitability.

Cycle 1: **REPAIR REQUIRED**, one HIGH financial-semantic finding.
`Service.capture_closing` calls `scenario_returns` without the saved profile's
no-tie semantics. An admitted synthetic postseason quote correctly produces one
entry scenario with V=74.00, but closing incorrectly becomes unavailable despite
six coherent quotes from three eligible books. With `tie_possible=False` the
same calculation produces V=74.00/stressed V=72.00. The default incorrectly demands
a tie return. Production impact is latent while the profile registry is empty.
This belongs to I02's payoff adaptation, not the later closing-cutoff repair.

Repair 1 passed the original no-tie probe. Cycle 2 found a second C01 failure:
an actual L=1 contradicting a known half-line profile's exact L=0 had no entry net,
but closing still computed a supported 31.25% ROI through the generic fallback.
`closing_contradiction_probe.py` reproduced it. Repair 2 changed the adapter to
an explicit financial-admissibility decision separate from tie presence. Known
incompatible/missing-proof profiles cannot calculate; unknown profiles retain
explicit diagnostics. Both probes now pass per author; focused 54/full 71 pass.
Targeted independent confirmation is pending; I02 gate remains open.
That confirmation passed both numerical probes and four closing tests, but found
one cause-reporting residual: an absent-profile moneyline missing T yields no ROI
but incorrectly claims missing references. `closing_missing_return_probe.py`
requires `OUTCOME_RETURN_REQUIRED`. After two repair cycles, Astra changed approach
to a complete service payoff preflight decision table across known/unknown profiles,
required/missing returns and missing references (D10), instead of another isolated
profile branch. Gate stays open; no assertion was weakened or failure relabeled.

All other reviewed A01-A09 financial expectations passed independent derivation:
fair -115 ROI, 12/23 margin removal, explicit W/T/L/stress, 9.59 strict ceiling,
zero tie-return example, coherent integer vectors/payoff disagreement/sign symmetry,
invalid input rejection, profile hash/version/admission/effective checks, full-payoff
and degenerate break-even, zero production profiles, and journalable contradictions.

Evidence `.qa/R02/verification.txt` records commands and environments:

- Focused source suite: 51 passed; full suite: 58 passed, two known warnings.
- `independent_financial_probe.py`: PASS.
- `actual_record_probe.py`: PASS.
- `closing_profile_probe.py`: expected assertion FAIL reproducing the finding.
- Compile and scoped diff checks: PASS.

Direct QuoteInput preserves entered numeric strings. SaveBet's current
rematerialization normalizes leading zeros; raw API/storage preservation is the
explicit I03 boundary. Source policy, temporal history, UI, closing cutoffs and
real-profile evidence remain unimplemented gates. No network, real database,
installation, commit, publication or global configuration mutation occurred.
