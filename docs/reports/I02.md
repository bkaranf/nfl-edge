# I02 financial containment implementation

Author: native `gpt-5.6-sol` / `xhigh`, session
`01a0a7e7-f8c4-7a43-9d3b-12a75cf5f635`, implementation turn
`01a0a7f0-bb17-7081-bb4f-f1c246b47976`. Base HEAD
`20e954bb175eaa8f3966ccf0327bb0b4476008bd` plus uncommitted I01.

Status: **ACCEPTED by fresh R02 for the financial scope**. The initial freeze
and repair history are preserved below. Final adapter-table verification passed
64 focused / 81 full tests; root independently reran all 81 at the push checkpoint.
Source/temporal/closing-quality and application-wide gates remain outstanding.

Explicit Decimal win/tie-or-push/loss returns replace assumed cash states. The
canonical/legacy bridge rejects conflicting duplicates and preserves monetary
strings. Known profile contradictions yield no financial calculation while actual
cash records remain journalable. Unsupported but calculable contracts are research.

Versioned settlement profiles validate hashes, evidence/admission/effective times,
exchange/market/period/season/overtime/tie semantics and cash constraints. Full
profile snapshots are emitted. The production registry is empty; admitted fixtures
are test-only. Integer vectors aggregate coherently, expose push/loss/payoff
dispersion, and can never meet the screen. General break-even and strict cent
ceilings handle degenerate payoffs and win-to-loss stress; exposure uses Decimal.

Owned files: backend/domain.py, engine.py, settlement_profiles.py; service.py
payoff adaptation only; tests/test_engine.py, test_financial_contracts.py and
financial fixtures in test_workflow.py. No storage/lifecycle/UI/provider changes.

Author verification: focused 51 passed; full isolated suite 58 passed; compileall
and scoped diff check exit 0. Evidence `.qa/I02/verification.txt`. Tests used fresh
temporary database directories and disabled collection. Existing dependency
deprecation warnings remain; no packages installed or upgraded.

Evidence correction: the author's shell log names `NFL_EDGE_DB_PATH`, which the app
does not use. Those two named paths are not proof of the test database location.
Actual collection is isolated by `tests/conftest.py`, which creates a distinct
`nfl-edge-pytest-*` TemporaryDirectory and sets `NFL_EDGE_DB` and `NFL_EDGE_NO_COLLECT`
before test module imports. R02 was informed; retain the original log with this
correction rather than silently rewriting historical evidence.

Initial freeze hashes (the service was subsequently repaired):

| File | SHA-256 |
|---|---|
| backend/domain.py | B574CD1D435E6C7465DAFD0240B2042BF7B077308659F2A56ED5F2D3CF764AFF |
| backend/engine.py | 9CB23BB89176B03E2E11419760FE72CC613C3495494365A7AABC503296525950 |
| backend/settlement_profiles.py | 24DEF7196EA49FDA22CBEF4F39C7B5790F2123CCE903C83A15D6AB4B5817B89A |
| backend/service.py | 6E2BBDCD7F61D9CFCB131A09A1F56CF179D2BCA5345B4A4DC55B5AA1B4709C27 |

Temporal source admission, immutable request persistence, journal linkage, closing
timing and UI are later tasks. These test passes do not establish those gates,
real profile/source eligibility, Windows release readiness or profitability.

Final service SHA-256:
`315C5D18A03BFCCEF24B4BD582681AFFD686330960709D22418D4F09D7C1B748`.
Final workflow SHA-256:
`E2959A5E658C8EF2041047C73C06B6417D63D6583B2581D1AD9AF60843D0B0F6`.
The new ten-row `tests/test_closing_adapter.py` hashes to
`8B3876D2A387B7AEB72644FF42D9D7D66EDCC5C16D3E5EE6C0C389098F5C138C`.
The adapter validates actual required W/L/T before benchmark selection, preserves
pinned tie rules, blocks contradictions/missing compatibility proof, and separates
missing economics from missing reference data. It does not consult the current
profile registry. All three independent closing probes passed. See R02.md and
local `.qa/I02-adapter-table/verification.txt` for the final acceptance evidence.
