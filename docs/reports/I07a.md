# I07a installed environment lock

Native `gpt-5.6-sol` / `xhigh`, default/openai; session
`01a0a80b-0c1f-7a31-99ca-3abb4e7d89f5`, repair turn
`01a0a813-b234-77e3-acf5-258bbbd28df2`. Root rechecked native metadata.

Status: **PASS for installed-environment consistency**, not clean installation.
Python 3.12.10, Node 24.14.1 and npm 11.11.0 are pinned. Direct Python dependencies
and the 23-package active Windows/test dependency closure use exact versions.
The editable project and unrelated environment tooling are excluded. Frontend
ranges/latest are replaced by installed versions; all 69 nested resolved npm
lock records remain unchanged from HEAD.

Changed files: pyproject.toml, requirements.lock, .python-version, .node-version,
frontend/package.json/package-lock.json, scripts/check_environment.py and
tests/test_environment.py. No backend/frontend application source edits.

The verifier reads package metadata and version commands, checks direct installed
packages and the npm dependency tree using offline `npm ls --all --json
--ignore-scripts --offline`. It never imports the app, initializes a DB, installs
packages or fetches network resources. Root review identified and the author fixed
an omitted-installed-record gap. Its regression removes React from a copied lock
under pytest tmp_path, leaving actual node_modules unchanged.

Evidence `.qa/I07a`: environment-check.log PASS, pytest.log 3 passed, npm build
PASS, npm tree PASS, nested-lock integrity PASS, compile/diff PASS. The repair
changed only verifier/tests; the prior build remains applicable. Root's combined
phase-1 suite also passed all 70 tests, including these three.

Limits: no clean install or download was authorized or performed. Python artifacts
are exact-version pinned, not hash-locked. Platform-specific optional npm packages
absent on this Windows host were not installed/executed. Windows launcher/restart/
backup/restore remain I07 and A26 work; this prerequisite does not clear them.
